# Data Pipeline Jobs

The current job catalog, inferred workflow order, data dependencies, and rerun notes are documented in [../../docs/jobs.md](../../docs/jobs.md).

This package contains plain Python `DataJob` subclasses. It does not currently provide a CLI, Django management command, scheduler, or production-safe orchestrator.